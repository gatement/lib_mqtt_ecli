{application, mqtt_cli,
 [
  {description, "An MQTT client library written with Erlang."},
  {vsn, "0.1.0"},
  {registered, []},
  {applications, [
                  kernel,
                  stdlib
                 ]},
  {mod, { mqtt_cli_app, []}},
  {env, [
      {test_mqtt_server, "localhost"}, 
      {test_mqtt_port, 1883}, 
      {test_mqtt_username, "guest"}, 
      {test_mqtt_password, "guest"}, 
      {test_mqtt_client_id, "johnson"}, 
      {test_mqtt_keep_alive, 600}
  ]}
 ]}.
